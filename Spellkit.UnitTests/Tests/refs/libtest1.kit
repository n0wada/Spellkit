import userName from libtest

let pi = 3.14

enum Asset { Computer(model, memory, rank), Phone(model, memory), Modem(model) }

func sayHello() {
    fmt("Hello, {0}", userName('J'+'o'+'n'))
}

func double(x) {
    "double " + x
}
