#region "Unicode identifiers"
    let 名前 = 42
    assert(42, 名前)

    func 合計(x, y) {
        x + y
    }

    let 値1 = 合計(20, 22)
    assert(42, 値1)

    let foo名前 = 1
    assert(1, foo名前)

    let _名前 = 2
    assert(2, _名前)
#endregion
